# python
__pycache__
*.py[cod]
*.egg
*.egg-info/

# tools
.mypy_cache/
.ruff_cache/
.pytest_cache/

# worktrees (and workspaces for jj)
.worktrees/
.workspaces/

# build
build/
dist/

# NOTE: the mazegen-*.whl / mazegen-*.tar.gz at the repo root are the
# reusable-package deliverable (subject Chapter VI) and are intentionally
# tracked so they land in the turn-in. Only the uv build scratch under
# dist/ (ignored above) is transient.

# coverage
.coverage
htmlcov/

# environment
.env
.venv/

# logs
*.log

# os
.DS_Store
Thumbs.db

# not mine
docs/42

# vendored MLX wheels — not needed in the turn-in
vendor/

# local / personal
CLAUDE.md
.claude/
